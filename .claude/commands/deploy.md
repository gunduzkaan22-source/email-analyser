# Deploy to Railway

Run the following steps to deploy the current project to Railway via GitHub:

1. Run `git status` to show what files have changed
2. Run `git add .` to stage all changes
3. Ask me: "What did you change in this session?" and use my answer as the commit message
4. Run `git commit -m "<my answer>"`
5. Run `git push origin main`
6. Tell me: "Deployed! Railway will update at email-analyser-production.up.railway.app in 2-3 minutes"
